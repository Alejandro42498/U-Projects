const path = require('path');

module.exports = {
    config: path.resolve('src/config', 'config.json'), // Ruta personalizada para config.json
    'models-path': path.resolve('src/models'),        // Ruta personalizada para modelos
    'migrations-path': path.resolve('src/migrations'), // Ruta personalizada para migraciones
    'seeders-path': path.resolve('src/seeders')       // Ruta personalizada para seeders
};
